spring.application.name=backend
# CORS Configuration
cors.allowed-origins=http://localhost:3000

# AI Scanner Service
ai.scanner.url=http://localhost:5000/api/analyze

# WebSocket Configuration
websocket.endpoint=/ws
websocket.allowed-origins=*

